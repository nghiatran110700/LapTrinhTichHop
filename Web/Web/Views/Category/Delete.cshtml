@model Web.Models.Entity.category

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>category</h4>
    <hr />

    <table class="table-fill" style="max-width:900px; width:70%;">
        <thead>
            <tr>
                <th class="text-left">Name </th>
                <th class="text-left">id</th>
                </th>
            </tr>

        </thead>
        <tbody class="table-hover">
            <tr>
                <td class="text-left">
                    @Html.DisplayNameFor(model => model.nameCategory)
                </td>
                <td class="text-left">
                    @Html.DisplayFor(model => model.nameCategory)
                </td>
            </tr>
        </tbody>
    </table>
    @*<dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.nameCategory)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.nameCategory)
            </dd>

        </dl>*@
    <div class="controll">
        @*<a href="@Url.Action("Edit","Admin" ,new { id = Model.idProduct})" class="link-edit">Edit</a>
            <a href="@Url.Action("Index","Admin")" class="link-edit">Back</a>*@
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="link-edit" />
                <a href="@Url.Action("Index","Category")" class="link-edit">Back</a>
            </div>
        }
    </div>

    @*@using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }*@
</div>
